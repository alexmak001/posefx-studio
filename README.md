# posefx-studio

Real-time webcam pipeline for body tracking, gesture detection, and live body-following visual effects.

## Setup

```bash
uv sync
```

## Run

```bash
uv run python -m src.main --config config/demo.yaml
```

Press `q` or `ESC` to quit.
